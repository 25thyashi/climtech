/* Apply styles to the whole page */
body {
    background-color: #FAF8F0; /* A warm off-white - adjust as needed! */
    font-family: 'DM Sans', sans-serif; /* Use DM Sans, fallback to generic sans-serif */
    font-size: 18px; /* Base font size - adjust as needed */
    line-height: 1.6; /* Improves readability */
    color: #333; /* Default text color (dark grey) */
    padding: 20px 40px; /* Add padding: 20px top/bottom, 40px left/right */
    margin: 0; /* Remove default body margin */
}

/* Style main headings */
h1 {
    font-size: 2.5em; /* Make H1 significantly larger (relative to body font size) */
    font-weight: 700; /* Use the bold weight we imported */
}

/* Add some space below paragraphs */
p {
    margin-bottom: 1em;
}

/* Style the Logo */
#logo {
    display: block; /* Make the image a block element */
    max-width: 150px; /* Limit logo size - adjust as needed */
    margin-bottom: 20px; /* Add space below the logo */
}

/* Style the Navigation Container */
nav {
    /* No specific styles needed yet for vertical layout, */
    /* but you could add width or margin here if required */
}

/* Style the Navigation List */
nav ul {
    list-style-type: none; /* Remove bullet points */
    padding-left: 0;       /* Remove default browser padding */
    margin: 0;             /* Remove default browser margin */
}

/* Style Individual Navigation Items */
nav li {
    margin-bottom: 10px; /* Add vertical spacing between links */
}

/* Style Navigation Links */
nav a {
    font-family: 'DM Mono', monospace; /* Use DM Mono, fallback to generic monospace */
    font-size: 1.1em;        /* Slightly larger than body text - adjust */
    text-decoration: none; /* Remove underline from links */
    color: #555;           /* Link color - adjust */
    display: block;        /* Make the whole link area clickable */
}

/* Style Link Hover State */
nav a:hover {
    color: #000;           /* Darken link color on hover */
    /* Add other hover effects if desired, e.g., background-color */
}

/* Style the Main Content Area */
main {
    margin-top: 30px; /* Add space between nav and main content */
}